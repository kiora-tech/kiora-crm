<form method="post" action="{{ path('project_delete', {'id': project.id}) }}" onsubmit="return confirm('{{ 'button.confirmation.delete'|trans }}');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
    <twig:Button
        type="submit"
        theme="danger"
        icon="trash"
        :label="'button.delete'|trans"
    />
</form>