{% extends 'base.html.twig' %}

{% block title %}{{ 'project.new'|trans }}{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ 'project.new'|trans }}</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('homepage') }}">{{ 'menu.home'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('project_index') }}">{{ 'menu.projects'|trans }}</a></li>
                <li class="breadcrumb-item active">{{ 'project.new'|trans }}</li>
            </ol>
        </nav>
    </div>

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ 'project.create_form'|trans }}</h5>
                        {{ include('project/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}