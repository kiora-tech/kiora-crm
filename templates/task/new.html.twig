{% extends 'base.html.twig' %}

{% block title %}{{ 'task.new'|trans }}{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ 'task.new'|trans }}</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('homepage') }}">{{ 'menu.home'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('task_index') }}">{{ 'task.list'|trans }}</a></li>
                <li class="breadcrumb-item active">{{ 'task.new'|trans }}</li>
            </ol>
        </nav>
    </div>

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ 'task.create_form'|trans }}</h5>
                        {{ include('task/_form.html.twig') }}
                        
                        <div class="mt-3">
                            <twig:Button
                                link="{{ path('task_index') }}"
                                theme="info"
                                icon="list"
                                :label="'button.back_to_list'|trans"
                            />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}