{% extends 'base.html.twig' %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ 'manage_client.assigned_collaborators.title'| trans }}</h1>
    </div>
    <section class="section">
        <div class="row">
            <div class="card p-4">
                <div class="card-body">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" id="save-button"
                            class="btn btn-primary">{{ 'button.save'| trans }}</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}