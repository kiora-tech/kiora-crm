{% extends 'base.html.twig' %}

{% block title %}{{ 'legal_person.edit'|trans }}{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ 'legal_person.edit'|trans }}</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('homepage') }}">{{ 'menu.home'|trans }}</a></li>
                <li class="breadcrumb-item">{{ 'menu.clients'|trans }}</li>
                <li class="breadcrumb-item"><a href="{{ path('legal_person_index') }}">{{ 'menu.legal_persons'|trans }}</a></li>
                <li class="breadcrumb-item"><a href="{{ path('legal_person_show', {'id': person.id}) }}">{{ person.name }}</a></li>
                <li class="breadcrumb-item active">{{ 'legal_person.edit'|trans }}</li>
            </ol>
        </nav>
    </div>

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ 'legal_person.edit_form'|trans }}</h5>
                        {{ include('legal_person/_form.html.twig', {'button_label': 'button.update'|trans}) }}
                        
                        <div class="d-flex gap-2 mt-3">
                            <twig:Button
                                link="{{ path('legal_person_show', {'id': person.id}) }}"
                                theme="secondary"
                                icon="eye"
                                :label="'button.back_to_details'|trans"
                            />
                            
                            <twig:Button
                                link="{{ path('legal_person_index') }}"
                                theme="info"
                                icon="list"
                                :label="'button.back_to_list'|trans"
                            />
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}