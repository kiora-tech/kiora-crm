{% extends 'base.html.twig' %}

{% block title %}{{ 'legal_person.new'|trans }}{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ 'legal_person.new'|trans }}</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('homepage') }}">{{ 'menu.home'|trans }}</a></li>
                <li class="breadcrumb-item">{{ 'menu.clients'|trans }}</li>
                <li class="breadcrumb-item"><a href="{{ path('legal_person_index') }}">{{ 'menu.legal_persons'|trans }}</a></li>
                <li class="breadcrumb-item active">{{ 'legal_person.new'|trans }}</li>
            </ol>
        </nav>
    </div>

    <section class="section">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ 'legal_person.create_form'|trans }}</h5>
                        {{ include('legal_person/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}