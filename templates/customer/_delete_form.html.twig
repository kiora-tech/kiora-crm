<form style="display: flex;justify-content: flex-end;" method="post" action="{{ path('app_customer_delete', {'id': customer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
    <button style="" class="btn btn-danger">{{ "button.delete"|trans }}</button>
</form>
