{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary">{{ "button.save"|trans }}</button>
<a href="#" data-turbo-action="remove" data-turbo-frame="energy" class="btn btn-secondary">{{ "button.close"|trans }}</a>
{{ form_end(form) }}
