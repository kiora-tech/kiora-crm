<div class="col-12">
    <div class="card recent-sales">
        <div class="card-body">
            <h5 class="card-title">{{ 'dashboard.history_signature.label'|trans }}</h5>
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                    <tr class="table-primary">
                        <th scope="col" class="text-start">{{ 'document_signature.index.signers'|trans }}</th>
                        <th scope="col" class="text-start">{{ 'document_signature.signature_request_status'|trans }}</th>
                        <th scope="col" class="text-end">{{ 'head.action'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>