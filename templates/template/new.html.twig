{% extends 'base.html.twig' %}

{% block title %}
    {{ "template.new"|trans }}
{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ "template.new"|trans }}</h1>
    </div>
    <section class="section">
        <div class="row">
            <div class="card p-4">
                <div class="card-body">
                    {{ include('template/_form.html.twig') }}
                    <a class="btn btn-primary" href="{{ path('app_template_index') }}">{{ "button.back"|trans }}</a>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
