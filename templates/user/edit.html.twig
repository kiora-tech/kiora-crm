{% extends 'base.html.twig' %}

{% block title %}{{ "button.update"|trans }}{% endblock %}

{% block body %}
    <div class="pagetitle">
        <h1>{{ "button.update"|trans }}</h1>
    </div>
    <section class="section">
        <div class="row">
            <div class="card p-4">
                <div class="card-body">
                    {{ include('user/_form.html.twig', {'button_label': "button.updateAll"|trans}) }}
                    <a class="btn btn-primary mt-3" href="{{ path('app_user_index') }}">{{ "button.back"|trans }}</a>
                    {{ include('user/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
