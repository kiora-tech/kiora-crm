<form method="post" action="{{ path('app_business_entity_delete', {'id': business_entity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ business_entity.id) }}">
    <button class="btn">Delete</button>
</form>
