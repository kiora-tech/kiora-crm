{% extends 'base.html.twig' %}

{% block title %}BusinessEntity{% endblock %}

{% block body %}
    <h1>BusinessEntity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ business_entity.id }}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ business_entity.siret }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_business_entity_index') }}">back to list</a>

    <a href="{{ path('app_business_entity_edit', {'id': business_entity.id}) }}">edit</a>

    {{ include('business_entity/_delete_form.html.twig') }}
{% endblock %}
